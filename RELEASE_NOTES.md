# v8.13 — فشرده‌سازی و بستن نشست

## 🗜 فشرده‌سازی (compact)
- دکمهٔ **🗜 فشرده‌سازی** در پیام `📊 وضعیت و هزینه` و همچنین زیر پیام پایان هر چت اضافه شد.
- با زدنش نشست روی سرور opencode **خلاصه می‌شود** تا مصرف توکنِ ادامهٔ گفتگو کم شود (معادل `/compact` در CLI).
- فشرده‌سازی در پس‌زمینه انجام می‌شود و نتیجه‌اش روی همان پیام گزارش می‌شود.

## ✖️ بستن نشست
- دکمهٔ **✖️ بستن نشست** کنار فشرده‌سازی/حذف اضافه شد: نشست از حالت فعال خارج (و اجرای در جریانش متوقف) می‌شود، ولی روی سرور باقی می‌ماند و از `🗂 نشست‌ها` قابل برگشت است.

## ⏱ ماندگاری دکمه‌های پایان چت
- دکمه‌های سریع پایان چت (فشرده‌سازی/بستن/حذف) حالا تا **۶۰ ثانیه** فعال می‌مانند (قبلاً ۱۰ ثانیه).

**Full Changelog**: https://github.com/Aknuun/Negahban-OpenCode/compare/v8.12...v8.13
